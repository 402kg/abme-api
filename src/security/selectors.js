export const SAFE_USER = `
    -_id
    about
    email
    links.name
    links.url
    name
    position
    projects.date
    projects.description
    projects.links
    projects.name
    projects.stack
    skills.name
    skills.props
    uuid
`

export default { }
